The teeing operation in Java Streams is a collector that allows you to split the input stream into two separate streams
and merge their results into a final output.
It is useful when you want to perform two operations on the same stream and combine their results.